<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Bluetooth-Codec-Erkennung (aptX/AAC/SBC) für die Audio-Ausgabe-Priorisierung -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Automatischer Download in Music/LetheMediaPlayer (nur vor Scoped Storage nötig) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Lokale Medien durchsuchen (MediaStore): ab Android 13 granulare Audio-Berechtigung -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Zugriff auf die Lethe-Session (nur bei identischer Signatur gewährt) -->
    <uses-permission android:name="com.Lethe.app.permission.AUTH_BRIDGE" />

    <!-- Package-Visibility (Android 11+): Lethe-Haupt-App + deren Konto-Brücke sichtbar machen -->
    <queries>
        <package android:name="com.Lethe.app" />
        <provider android:authorities="com.Lethe.app.authbridge" />
    </queries>

    <application
        android:name=".MediaPlayerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LetheMediaPlayer"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Gleiche Cast-Receiver-ID wie die Haupt-App -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.lethe.mediaplayer.cast.CastOptionsProvider" />

        <!-- Android Auto: Player erscheint als Medien-App, Browse-Baum siehe PlaybackService -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.LetheMediaPlayer"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Registrierung als Musik-Player: erscheint bei "Öffnen mit" für Audiodateien -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>

            <!-- Song-Teilen-Link (Custom-Scheme): lethemp://song?u={audioUrl} öffnet den Player mit dem
                 geteilten Titel. Wird von der Web-Fallback-Seite song.php aufgerufen und öffnet die App
                 auch dann zuverlässig, wenn der https-App-Link (unten) nicht verifiziert ist. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lethemp" android:host="song" />
            </intent-filter>

            <!-- Song-Teilen-Link (App-Link): https://letheapp.de/song.php?u={audioUrl} öffnet bei
                 verifizierter Domain direkt den Player. Ohne App/Verifikation liefert der Server die
                 Web-Fallback-Seite (Web-Wiedergabe + APK-Download). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="letheapp.de" android:pathPrefix="/song.php" />
            </intent-filter>
        </activity>

        <!-- Hintergrund-Wiedergabe via MediaLibraryService (Media-Notification, Lockscreen, Auto).
             Der zusätzliche Legacy-Action android.media.browse.MediaBrowserService ist ZWINGEND,
             damit Android Auto / Android Automotive OS den Player als browsebare Medien-App
             erkennt und ihn im Auto-Launcher anzeigt (siehe Browse-Baum in LibrarySessionCallback). -->
        <service
            android:name=".player.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media-Button-Empfänger: ZWINGEND, damit das System / Android Auto die zuletzt gehörte
             Wiedergabe fortsetzen kann (löst onPlaybackResumption im PlaybackService aus, auch wenn
             der Dienst gerade nicht läuft). Ohne diesen Empfänger zeigt Android Auto beim Einsteigen
             nur "Tippen zum Öffnen" statt automatisch weiterzuspielen. -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- FileProvider für generierte Teilen-Bilder (Song-Sharepic) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>
    </application>

</manifest>
